[project]
name = "the-tartan-snipes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "slack-bolt>=1.28.0",
    "slack-sdk>=3.41.0",
]
